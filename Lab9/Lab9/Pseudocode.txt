Функция РазделитьСтроки (Строка стрВвд, Адрес строки стрВывд, Адрес строки стрМакс)
    Строка стрТекущ
    ПотокСтрок стрПоток
    стрПоток << стрВвд
    
    Булевая переменная Аут = Правда
    Цикл Пока стрПоток >> стрТекущ Выполнить:
        Если Аут:
            (*стрВывд) += стрТекущ + ' '
            Если (Длина(стрТекущ) > Длина(*стрМакс))
                (*стрМакс) = стрТекущ
            Конец условия
        Конец условия
        Аут = !Аут
    Конец цикла
Конец Функции

Начало
    Строка стрВвд
    Строка стрМакс
    Строка стрВывд

    Вывод: Введите строку для обработки
    ПолучитьСтроку (Ввод, стрВвд)
    
    РазделитьСтроки (стрВвд, &стрВывд, &стрМакс)

    Вывод: Вот введённая строка без слов на чётных местах
    Вывод: стрВывд
    Вывод: Вот самое длинное слово среди оставшихся
    Вывод: стрМакс
Конец